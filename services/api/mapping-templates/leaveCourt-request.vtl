#if( $ctx.args.input.memberId != $ctx.identity.username )
	$util.unauthorized()
#end

{
    "version" : "2017-02-28",
    "operation" : "DeleteItem",
    "key" : {
        "courtId" : { "S" : "${ctx.args.input.courtId}" },
        "memberId" : { "S" : "${ctx.args.input.memberId}" }
    }
}