#if( $ctx.args.input.followerId != $ctx.identity.username )
	$util.unauthorized()
#end

{
    "version" : "2017-02-28",
    "operation" : "PutItem",
    "key" : {
        "followerId": $util.dynamodb.toDynamoDBJson($ctx.args.input.followerId),
        "followeeId": $util.dynamodb.toDynamoDBJson($ctx.args.input.followeeId)
    }
}