#if ($ctx.args.userId != $ctx.identity.username)
	$util.unauthorized()
#end

{
    "version": "2017-02-28",
    "operation": "GetItem",
    "key": {
        "userId": $util.dynamodb.toDynamoDBJson($ctx.args.userId),
    }
}