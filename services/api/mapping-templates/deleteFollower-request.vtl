#if( $ctx.args.input.followerId != $ctx.identity.username )
	$util.unauthorized()
#end

{
    "version" : "2017-02-28",
    "operation" : "DeleteItem",
    "key" : {
        "followerId" : { "S" : "${ctx.args.input.followerId}" },
        "followeeId" : { "S" : "${ctx.args.input.followeeId}" }
    }
}